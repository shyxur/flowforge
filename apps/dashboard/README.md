# QueueFlow Dashboard

Next.js operations dashboard for FlowForge QueueFlow.

```bash
cp .env.example .env.local
npm install
npm run dev
```

The dashboard API base URL is read from `QUEUEFLOW_API_BASE_URL`.
