# Dashboard screenshots

No verified dashboard screenshots are committed yet.

Add screenshots captured from a real windylane development environment here.
Use representative, non-sensitive data; redact API keys, webhook signing
secrets, customer payloads, and other credentials before committing.

Suggested files:

- `tasks.png`
- `task-detail.png`
- `webhooks.png`
- `webhook-deliveries.png`

Keep screenshots aligned with the current monochrome brand system and replace
outdated captures when the dashboard changes. Do not use mockups or generated
screenshots as product evidence.
